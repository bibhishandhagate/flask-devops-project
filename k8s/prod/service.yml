apiVersion: v1
kind: Service
metadata:
  name: flask-app-prod-service
  namespace: prod
spec:
  type: NodePort
  selector:
    app: flask-app
    env: prod
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30002
