apiVersion: v1
kind: Service
metadata:
  name: flask-app-dev-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: flask-app
    env: dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30001
